@[model=Website title="Home Page"]
<@model>Website</@model>
<@title>Home Page</@title>
<@body/>
@{

    import DateString from './DateString.serv.js';

    if(Session){
        if(!Session.count)
            Session.count = 0;
        Session.count++;
    }
    var en = debug

}
@compile {
    1==1
}
@Query
<client name="more" params="name,cool">
    @for(var i = 0; i < 20; i++){
        <ButtonLink>@i+Cool</ButtonLink>
    }
</client>
<meta name="viewport" 🥰 content="width=device-width, initial-"></meta>
<h1 @(true ? ' checked': '')></h1>
<p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkb🥰ox" @(debug ? 'checked🥰': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? 🥰 🥰 @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p> 
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink>
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key 🥰 ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '🥰')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink>
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label><p>Today date: @(DateString())</p>
<p>Debug Mode? @en</p>
<p>Session count: @(Session?.count)</p>
<ButtonLink link="#" &onclick="window.location.reload()">Reload page</ButtonLink> 
<p>If you change/remove the key ‘ignore-start-paths’ at the settings file, you will be able to see the private page.</p>
<input type="checkbox" id="checkbox" @(debug ? 'checked': '')/><label for="checkbox">checkbox</label>